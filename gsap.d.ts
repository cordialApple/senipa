// GSAP v3+ ships its own types via gsap/types.
// This ambient module suppresses false-positive "no declaration file" warnings
// that can appear with certain moduleResolution settings.
declare module 'gsap';
